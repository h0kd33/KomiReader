<?xml version="1.0" encoding="utf-8"?>
<moe.komi.reader.board.BoardFooterView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="90dp"
    android:layout_gravity="center|top"
    android:background="@color/bgcolor"
    android:gravity="center|top"
    android:layout_marginBottom="0dp"
    android:layout_marginTop="0dp"
    android:layout_marginRight="0dp"
    android:layout_marginLeft="0dp">

    <!--
    <com.pnikosis.materialishprogress.ProgressWheel
        xmlns:wheel="http://schemas.android.com/apk/res-auto"
        android:id="@+id/progress_wheel"
        android:layout_width="80dp"
        android:layout_height="80dp"
        android:layout_centerHorizontal="true"
        android:layout_alignParentTop="true"
        wheel:matProg_barColor="@color/post_text_color"
        wheel:matProg_progressIndeterminate="true" />
    -->

    <Button
        android:id="@+id/next_page_button"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:background="@color/post_text_color"
        android:padding="10dp"
        android:text="@string/next_page"
        android:textColor="@android:color/white"
        android:textSize="@dimen/abc_text_size_large_material" />

    <TextView
        android:id="@+id/nomore_text"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:gravity="center"
        android:paddingTop="10dp"
        android:text="@string/nomore"
        android:textSize="@dimen/abc_text_size_large_material"
        android:visibility="gone" />
</moe.komi.reader.board.BoardFooterView>